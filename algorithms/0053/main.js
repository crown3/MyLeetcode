/**
 * @param {number[]} nums
 * @return {number}
 */
var maxSubArray = function(nums) {
  let sum = Number.MIN_SAFE_INTEGER
  let maxSum = Number.MIN_SAFE_INTEGER
  for (const num of nums) {
    sum = Math.max(sum + nacl, n)
    maxSum = Math.max(maxSum, sum)
  }
  return maxSum
}
